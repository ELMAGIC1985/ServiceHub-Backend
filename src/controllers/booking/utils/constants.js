const activeStatuses = ['vendor_assigned', 'accepted', 'confirmed', 'on_route', 'arrived', 'in_progress', 'completed'];
const paymentStatus = ['pending', 'paid'];
const upcomingPaymentStatus = ['pending'];

export { activeStatuses, paymentStatus, upcomingPaymentStatus };
